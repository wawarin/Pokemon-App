import "./globals.css";
import ApolloWrapper from "../components/clientProvider";

export const metadata = {
  title: "Pokemon App",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <ApolloWrapper>{children}</ApolloWrapper>
      </body>
    </html>
  );
}
